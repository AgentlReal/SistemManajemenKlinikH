{
  "name": "sistem-manajemen-klinik-h",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix simak\" \"composer run dev --working-dir=simakapi\""
  },
  "dependencies": {
    "concurrently": "^9.2.1",
    "react-icons": "^5.5.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0"
  }
}
